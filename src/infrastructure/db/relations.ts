/*class Relations {
    private db: any;

    constructor(db) {
        this.db = db;
    }

    load() {
        this.db.main.invoice.belongsTo(this.db.main.client, {
            constraints: false,
            foreingKey: 'product_id'
        });
    }
}

module.exports = Relations;*/